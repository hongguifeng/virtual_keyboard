# Windows 智能悬浮虚拟键盘软件功能规格说明

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 文档版本 | 1.0 |
| 文档状态 | 开发基线草案 |
| 编制日期 | 2026-09-06 |
| 目标版本 | MVP 1.0 |
| 关联文档 | [方案设计文档](./Windows%20智能悬浮虚拟键盘方案设计文档.md)、[开发计划 TODO](./Windows%20智能悬浮虚拟键盘开发计划%20TODO.md) |

## 2. 文档目的

本文档定义 Windows 智能悬浮虚拟键盘 MVP 的产品范围、用户行为、功能需求、非功能需求和验收标准。本文档回答“软件必须做什么”，不规定具体类、API 或代码实现。

需求使用以下标识：

- `FR-*`：功能需求。
- `NFR-*`：非功能需求。
- `AC-*`：验收场景。
- `P0/P1/P2`：必须、应当、可延期。

## 3. 产品概述

### 3.1 产品目标

软件作为一个长期运行的 Windows 桌面辅助工具，在用户把输入焦点切换到可编辑文本控件时，自动显示一个不抢占焦点的悬浮虚拟键盘。用户通过鼠标或触摸点击虚拟按键，将文本、按键或组合键发送到当前有效的目标输入控件。

MVP 的目标是：

1. 在受支持的普通桌面应用中可靠识别文本输入状态。
2. 显示键盘时不破坏目标应用的前台状态和输入焦点。
3. 避免把输入发送到已经失效或已经切换的目标窗口。
4. 在多显示器、不同 DPI 环境中将键盘放置在可见且尽量不遮挡输入位置的区域。
5. 允许用户配置尺寸、透明度和 JSON 键盘布局。
6. 对无法检测或无法注入的场景给出明确、可诊断的结果，而不是静默误操作。

### 3.2 MVP 技术与平台基线

- 操作系统：Windows 10 22H2、Windows 11，首发支持 x64。
- 应用形态：C#、WPF、.NET 10 LTS 桌面应用。
- 目标应用：与本程序处于相同或更低完整性级别的 Win32、WinForms、WPF、Chromium/Electron 桌面应用。
- 输入设备：MVP 必须支持鼠标；触摸点击作为 P1 验证项。
- 输入语言：标准键盘按键以当前目标线程的 Windows 键盘布局为准；自定义文本支持 Unicode。

### 3.3 明确不承诺的范围

以下内容不属于 MVP 的兼容性承诺：

- UAC 安全桌面、登录桌面、锁屏和 Ctrl+Alt+Delete 安全序列。
- 从普通权限向管理员权限窗口注入输入。
- 游戏、反作弊软件、远程控制软件或主动拒绝合成输入的应用。
- 所有第三方 UI Automation provider 的无条件兼容。
- 手写、语音、候选词、拼写纠正和完整输入法编辑器。
- 任意 Shell 命令或脚本执行。
- 在只读文档、网页正文或仅支持 TextPattern 的元素上自动显示。

如后续要求支持管理员窗口，必须单独评审 `uiAccess`、代码签名、可信安装目录和安全风险。

## 4. 术语与行为约定

| 术语 | 定义 |
|---|---|
| 目标元素 | 当前持有系统键盘焦点、并被判定为可编辑的 UI Automation 元素。 |
| 目标会话 | 对目标元素、顶层窗口、进程、坐标和检测时间的一次快照，用于防止误输入。 |
| 可编辑 | 有足够正向证据表明能够接收文本编辑输入；仅支持 TextPattern 不等同于可编辑。 |
| 焦点离开 | 系统键盘焦点从目标元素切换到另一个非可编辑元素、另一个应用或无有效目标。 |
| 手动抑制 | 用户主动关闭键盘后，针对当前目标暂时禁止自动重新显示。 |
| 文本动作 | 提交一个 Unicode 字符串，不要求模拟具体物理键。 |
| 按键动作 | 模拟一个具体虚拟键或扫描码。 |
| 热键动作 | 按确定顺序发送多个修饰键和普通按键。 |

自动隐藏以“焦点是否仍在可编辑目标”为准。若用户点击窗口空白处但输入焦点仍留在原输入框，MVP 不强制隐藏键盘；MVP 不安装全局低级鼠标钩子来推断点击意图。

## 5. 用户角色与典型场景

### 5.1 用户角色

- 主要用户：依赖鼠标或触摸完成文本输入、需要可配置快捷按键的 Windows 用户。
- 高级用户：创建自定义布局、短语键和组合键的用户。
- 支持人员：通过不包含输入内容的诊断日志排查兼容性问题。

### 5.2 核心用户旅程

1. 用户启动程序，程序进入托盘并等待输入焦点变化。
2. 用户点击记事本、浏览器或编辑器中的输入区域。
3. 程序识别目标、确定插入位置或控件位置，在目标显示器显示键盘。
4. 用户点击虚拟按键，目标应用保持焦点并接收输入。
5. 用户切换到按钮、菜单、另一个应用或关闭目标窗口，键盘自动隐藏或重新定位。
6. 用户可主动关闭键盘、从托盘重新显示、修改尺寸、透明度或布局。

## 6. 功能需求

### 6.1 应用生命周期与托盘

#### FR-APP-001（P0）单实例运行

系统必须保证同一 Windows 用户会话中仅运行一个主实例。再次启动时，应将设置窗口或托盘入口带到用户可访问状态，不得启动第二套监听器。

#### FR-APP-002（P0）托盘入口

系统必须提供托盘菜单，至少包含：

- 启用/暂停自动键盘。
- 显示当前键盘。
- 打开设置。
- 重新加载布局。
- 退出。

#### FR-APP-003（P0）安全退出

退出时必须注销 UI Automation 事件、取消待处理输入、释放修饰键状态、保存配置并关闭所有窗口。

#### FR-APP-004（P2）开机启动

可允许用户启用当前用户级开机启动。MVP 首次启动不得默认启用。

### 6.2 焦点监听与目标识别

#### FR-FOC-001（P0）监听输入焦点

启用状态下，系统必须监听全局 UI Automation 焦点变化，并在焦点稳定后评估新元素。对于不可靠或不发送焦点事件的 provider，必须以有界的当前焦点轮询作为兜底；等价焦点快照不得重复触发目标会话和重绘。监听与轮询均不得阻塞 WPF UI 线程。

#### FR-FOC-002（P0）忽略自身元素

自动检测必须忽略虚拟键盘自身进程中的键盘窗口。设置窗口打开期间不得因为设置控件获得焦点而显示另一套虚拟键盘。

#### FR-FOC-003（P0）可编辑性判定

系统必须将候选元素分类为 `Editable`、`NotEditable` 或 `Unknown`。判定规则必须满足：

- 元素必须有效、已启用并持有键盘焦点。
- `ControlType.Edit` 上的 `ValuePattern.IsReadOnly == false` 是正向证据；非编辑控件不得仅凭 ValuePattern 判定为输入目标。
- Edit/Document 上的 `TextEditPattern` 是正向证据之一。
- 可编辑的 Edit 控件和密码 Edit 控件可作为正向证据。
- TextPattern 或 TextPattern2 单独存在时不得直接判定为可编辑。
- 只读、禁用、离屏、已销毁或属于本程序的元素不得判定为可编辑。
- 桌面图标、文件资源管理器文件项等选择型控件不得触发键盘；进入文件重命名等真实 Edit 状态后才可触发。
- 对 Document 类复杂编辑器，可结合有效 caret/selection 坐标形成正向证据。

#### FR-FOC-004（P0）兼容性降级

当 UI Automation 不能提供完整信息时，系统可使用前台窗口、焦点 HWND 或 caret 坐标补充目标和定位信息，但不得仅凭窗口标题、进程名或 TextPattern 判定可编辑。

#### FR-FOC-005（P0）目标会话

识别成功后，系统必须创建目标会话，至少记录：

- 顶层窗口句柄。
- 进程 ID。
- UIA RuntimeId 或等价身份信息（若可用）。
- 元素类型、是否密码字段。
- 锚点矩形和检测时间。

目标会话失效后不得继续用于输入。

目标会话必须由焦点监听与分类流程自动建立；最终用户界面不得要求手动“捕获目标”，也不得展示 PID、HWND 等内部会话身份。

#### FR-FOC-006（P1）焦点事件防抖

系统必须合并短时间内的重复焦点事件，并确保较旧评估结果不能覆盖较新的焦点状态。

### 6.3 显示、隐藏与状态

#### FR-VIS-001（P0）自动显示

当系统处于启用状态，且当前焦点元素被判定为 Editable 时，必须显示键盘或更新已显示键盘的位置和目标会话。

#### FR-VIS-002（P0）自动隐藏

启用自动隐藏时，出现以下任一情况必须隐藏键盘：

- 焦点切换到 NotEditable 元素。
- 前台应用切换且新应用没有有效 Editable 目标。
- 目标窗口或目标元素被销毁。
- 程序被暂停。

仅发生鼠标点击但焦点仍停留在原目标时，不触发强制隐藏。

#### FR-VIS-003（P0）不激活窗口

显示、移动和点击键盘不得使键盘窗口成为前台激活窗口，不得夺走目标应用的键盘焦点。

#### FR-VIS-004（P0）手动关闭和重新显示

用户点击关闭按钮后，系统进入针对当前目标的 ManuallySuppressed 状态。以下事件之一解除该状态：

- 用户通过托盘选择“显示当前键盘”。
- 焦点离开当前目标后进入另一个 Editable 目标。
- 用户暂停后重新启用自动键盘。

#### FR-VIS-005（P0）置顶显示

键盘在可见期间必须位于普通目标窗口之上，但不得尝试覆盖安全桌面。置顶操作不得激活键盘。

#### FR-VIS-006（P1）拖动与缩放

用户必须能拖动标题栏移动键盘，并能直接拖动窗口任一边缘或角调整尺寸，缩放过程不得激活键盘或抢走输入焦点。交互缩放完成后必须把 DIP 宽高持久化到当前用户配置。手动移动后，系统应在当前目标会话内尊重用户位置；目标变化后是否恢复自动定位由设置决定。

### 6.4 智能定位与多显示器

#### FR-POS-001（P0）锚点选择

系统必须按以下优先级选择输入锚点：

1. 当前 caret 或 UIA selection 的有效屏幕矩形。
2. Win32 caret 的有效屏幕矩形。
3. 可编辑控件的 BoundingRectangle。
4. 当前目标显示器的安全默认锚点。

#### FR-POS-002（P0）候选位置

系统必须至少评估下、上、右、左四个候选位置，优先选择不覆盖锚点、完全位于工作区、且距离锚点较近的位置。上方和下方位置必须在用户边距之外额外保留固定、随 DPI 缩放的 IME 候选窗安全区，避免第三方候选窗覆盖键盘并抢走按键点击；该安全区不得读取、记录或控制候选内容。

#### FR-POS-003（P0）工作区约束

最终键盘矩形必须被限制在目标显示器工作区内，不能落到屏幕外。任务栏位于任意边缘时均应正确处理。

#### FR-POS-004（P0）多显示器

键盘必须显示在目标锚点所在显示器。必须支持显示器位于主屏左侧或上方造成的负屏幕坐标。

#### FR-POS-005（P0）混合 DPI

系统必须在 100%、125%、150%、175%、200% DPI，以及不同 DPI 显示器之间正确转换物理像素和 WPF DIP。跨屏移动时必须重新计算尺寸和位置。

#### FR-POS-006（P1）过大键盘处理

当配置尺寸大于目标显示器工作区时，系统必须缩小到安全上限并保持最小可操作按键尺寸，不得生成不可恢复的屏外窗口。

### 6.5 键盘布局与交互

#### FR-KEY-001（P0）标准布局

MVP 必须提供英文 QWERTY 布局，至少包含：

- 字母 A-Z。
- 数字 0-9 及标准美式键盘主键区标点键。
- Space、Backspace、Enter、Tab、Escape。
- Shift、Ctrl、Alt、Win、Fn、CapsLock。
- 左、右、上、下方向键。
- 主键区必须按标准美式 QWERTY 行顺序排列，方向键必须采用倒 T 排列；Up 与 Down 的水平中心必须完全一致，宽键和左右成对修饰键的位置应与标准 PC 键盘一致。
- 数字行通过 Fn 层提供 F1-F12；Fn 是应用内部功能层开关，不模拟厂商专用的物理 Fn 信号。
- 关闭、拖动和设置入口。

#### FR-KEY-002（P0）JSON 驱动

按键行、标签、宽度权重和动作必须由带版本号的 JSON 布局定义。程序不得在视图层硬编码完整键位集合。

#### FR-KEY-003（P0）按键动作类型

MVP 支持以下动作：

- `text`：Unicode 文本。
- `key`：单个虚拟键或扫描码。
- `hotkey`：兼容“修饰键 + 主键”的组合键。
- `chord`：按实际按下顺序记录的完整组合键，包含 1–8 个不同按键。
- `modifier`：Shift、Ctrl、Alt、Win、Fn 等状态键；Fn 仅切换布局的 `fnVirtualKey` 功能层。

MVP 不支持执行任意命令或脚本。

#### FR-KEY-004（P0）相对布局

每行按键宽度必须根据权重计算。固定尺寸控件可以单独声明最小/最大宽度，但不得依赖单一 DPI 下的绝对像素。

#### FR-KEY-005（P0）非焦点交互

普通按键控件不得请求键盘焦点，不得进入 Tab 导航。鼠标按下、释放和取消必须形成明确的视觉状态，且同一次点击最多触发一次输入动作；Backspace 的明确长按重复行为除外。

#### FR-KEY-006（P1）触摸交互

在触摸屏上，单指点击必须等价于鼠标点击；拖动区域与按键区域必须避免手势冲突。

### 6.6 输入发送与状态键

#### FR-INP-001（P0）发送前校验目标

每次输入批次开始前，系统必须确认：

- 当前前台顶层窗口仍属于目标会话。
- 当前焦点仍是原目标或其可接受的后继元素。
- 键盘窗口没有成为激活窗口。
- 目标会话未超时、未被销毁或被新会话替换。

校验失败时必须取消输入，不得尝试通过激活目标窗口来强行发送。

#### FR-INP-002（P0）Unicode 文本

`text` 动作必须支持 Unicode 字符串，包括 UTF-16 代理对。程序不得记录文本值。文本动作不承诺经过目标应用的 IME 组合过程。

#### FR-INP-003（P0）普通按键

`key` 动作必须在一次有效点击完成后，把 KeyDown/KeyUp 作为同一个不可交错的输入批次成对发送；释放到键外、捕获丢失或取消时不得发送。字母、数字和标点键不得进入锁存状态。扩展键、扫描码和目标线程当前键盘布局必须得到正确处理。普通键的 Down/Up 不得拆成跨越 IME 候选窗状态变化的两个系统调用。

Backspace 短按必须只删除一次；持续按住 450ms 后必须开始重复删除，重复间隔从 140ms 逐步缩短且不得低于 45ms。长按已经触发重复后，松手不得再附加一次删除。移出按键、捕获丢失、触摸取消、控件隐藏/卸载、目标变化或退出必须停止重复。任一时刻最多允许一个重复删除输入在途，不得因目标响应较慢形成松手后的积压删除。

#### FR-INP-004（P0）组合键

`hotkey` 和 `chord` 动作必须按定义顺序按下、逆序释放；`chord` 不得限制为一个修饰键加一个非修饰键，必须支持 `Win+Tab` 及多个普通键。发生异常、取消或退出时，程序必须尽最大努力释放由程序按下但尚未释放的全部按键。

#### FR-INP-005（P0）修饰键、Fn 与 CapsLock

- Shift、Ctrl、Alt、Win 均采用点击切换：第一次点击必须向目标发送并保持真实 KeyDown，再次点击同类键必须发送 KeyUp；只有原生发送成功后才更新界面状态。
- 保持状态必须参与后续每一个按键动作，不得在普通键后自动清除；例如 Shift 保持时，数字行按键必须由系统键盘布局解释为对应符号。
- Fn 同样采用点击切换，但只切换应用内部功能层；启用时带 `fnVirtualKey` 的键必须显示并发送替代键。
- 目标变化、暂停、设置打开或退出时必须清除全部修饰键和 Fn 保持状态，不能跨目标泄漏。
- 保持状态必须采用明显的背景色、前景色和边框区分，不能只依赖轻微透明度变化。
- CapsLock 必须明确采用系统状态或虚拟状态；MVP 默认读取并切换系统 CapsLock 状态。
- 实体键盘改变锁定状态后，虚拟键盘显示必须在下一次状态刷新时同步。

#### FR-INP-006（P0）输入失败反馈

当 `SendInput` 返回失败、目标校验失败或因权限边界无法输入时，系统不得假装成功。必须提供非侵入式状态提示，并写入不包含输入内容的诊断事件。

#### FR-INP-007（P0）密码字段保护

密码字段中仅允许标准字符和编辑按键；默认禁用自定义短语及可能泄漏数据的热键。日志不得记录密码字段的名称、Value 或输入内容。

### 6.7 设置与持久化

#### FR-CFG-001（P0）设置项

设置界面至少提供：

- 启用/暂停。
- 自动显示、自动隐藏。
- 键盘宽度和高度（单位为 DIP）。
- 透明程度（0% 表示完全不透明）。
- 锚点间距（DIP）。
- 布局选择。
- 拖动位置保留方式，并在设置界面直接说明“当前输入框”和“持续保留”的差异。
- 最多 12 个自定义按键；用户界面只区分输入文字和录制按键/组合键，新录制组合内部保存为 `chord` 动作，旧 `key`/`hotkey` 配置继续兼容。
- 录制按键/组合键时，用户点击录制后直接按实体键盘组合；系统必须记录首次 KeyDown 的完整先后顺序，以全部按键松开为一次录制结束，不得拆分为“修饰键 + 一个主键”。录制期间必须抑制系统处理，以便捕获 `Win+Tab` 等系统快捷键。
- 自定义按键必须位于标准键盘右侧，每列最多 5 个；放不下时向右新开一列，不得使用滚动条。标准区和自定义区必须随窗口可用宽度按比例共同缩放，窄窗口下不得重叠或覆盖；密码目标中全部隐藏。

#### FR-CFG-002（P0）透明度安全范围

用户必须通过带百分比反馈的滑动条设置整个窗口的透明程度，范围限制在 0%-70%。0% 必须表示完全不透明，数值升高时窗口整体逐渐透明；不得把该设置实现为亮度或背景色明暗调整。内部 WPF `Window.Opacity` 对应范围为 1.00–0.30。

#### FR-CFG-003（P0）用户数据目录

用户配置和自定义布局必须保存在当前用户的 `%LocalAppData%\VirtualKeyboard\` 下。安装目录中的内置布局视为只读资源。

#### FR-CFG-004（P0）配置可靠性

配置必须带 schemaVersion，保存必须采用原子替换。配置损坏或字段无效时，程序必须备份异常文件、加载安全默认值并提示用户，不得启动失败。

#### FR-CFG-005（P1）热重载

用户从托盘执行“重新加载布局”后，系统应校验并应用新布局；校验失败时继续使用最后一个有效布局。

### 6.8 诊断与隐私

#### FR-DIA-001（P0）结构化诊断

系统必须记录结构化诊断事件，包括时间、版本、模块、事件类型、目标进程 ID、控件类型、判定结果、错误码和耗时。

#### FR-DIA-002（P0）禁止记录敏感内容

任何日志均不得记录：

- 用户输入的字符或字符串。
- 剪贴板内容。
- 密码字段 Value。
- 自定义短语的实际值。

#### FR-DIA-003（P1）诊断开关与导出

用户可以启用详细诊断并导出日志。详细诊断默认关闭，日志必须轮转并限制总大小。

## 7. 状态模型

| 状态 | 含义 | 主要进入条件 | 主要退出条件 |
|---|---|---|---|
| Disabled | 自动功能暂停 | 用户暂停、系统退出中 | 用户启用 |
| Hidden | 已启用但无可见键盘 | 无有效目标、自动隐藏 | 发现 Editable 目标 |
| Evaluating | 正在评估最新焦点 | 收到有效焦点事件 | Editable、NotEditable、Unknown |
| VisibleTracking | 键盘可见并跟踪目标 | Editable 判定成功 | 失焦、目标失效、手动关闭 |
| ManuallySuppressed | 当前目标被用户主动隐藏 | 点击关闭按钮 | 新 Editable 目标、托盘显示、重新启用 |
| SettingsOpen | 设置窗口可激活 | 用户打开设置 | 关闭设置后按当前焦点重新评估 |

所有状态转换都必须携带单调递增的评估版本号；旧版本异步结果不得覆盖新状态。

## 8. 非功能需求

### NFR-PERF-001 性能

- 正常机器上，从稳定焦点事件到键盘可见的 P95 延迟应不超过 200 ms。
- 空闲状态平均 CPU 占用目标低于 1%。
- MVP 稳态工作集目标不超过 150 MB。
- 重复 UIA 事件不得导致无界队列或持续重绘。

### NFR-REL-001 可靠性

- UIA provider 抛出异常或元素失效不得导致主进程崩溃。
- 键盘显示、隐藏和位置计算必须幂等。
- 输入动作必须串行化，单次点击不得重复提交。
- 配置损坏、布局损坏和日志目录不可写均不得阻止程序进入可操作状态。

### NFR-SEC-001 安全

- 默认以普通用户权限运行，不自动请求管理员权限。
- 不绕过 UIPI，不尝试操作安全桌面。
- 不实现任意命令执行。
- 发布包必须支持代码签名；若未来启用 uiAccess，必须经过单独威胁建模。

### NFR-PRI-001 隐私

- 软件不得持久化用户实际输入内容。
- 不进行网络上传。若未来增加遥测，必须默认关闭并单独取得用户同意。

### NFR-COMP-001 兼容性

MVP 测试矩阵至少覆盖：

- Windows 10 22H2、Windows 11 当前受支持版本。
- 100%、125%、150%、200% DPI。
- 单屏、双屏、主屏左右均存在负坐标的布局。
- Notepad、WPF/WinForms 测试宿主、Chrome 或 Edge、VS Code。
- 普通输入框、只读输入框、密码框、网页 input/textarea、复杂编辑器。

Office 作为兼容性验证项，不作为没有明确版本环境时的自动化阻断项。

### NFR-MNT-001 可维护性

- UIA、分类、状态管理、定位、窗口、输入发送、布局和配置必须通过接口解耦。
- 核心分类与定位算法必须可在无桌面环境下进行单元测试。
- 原生 API 封装必须集中管理，不在视图代码中散布 P/Invoke。

### NFR-ACC-001 可用性与可访问性

- 按键标签在支持的 DPI 下必须清晰可辨。
- 关键操作必须有 ToolTip 或可理解的图标/文本。
- 对比度和按键最小可点击区域应满足常见触摸使用要求。
- NoActivate 键盘本身不承诺通过实体键盘进行 Tab 导航；设置窗口必须支持标准键盘导航。

## 9. MVP 验收场景

### AC-001 Notepad 端到端输入

给定程序已启用，用户聚焦 Notepad 编辑区；键盘应在 200 ms P95 目标内出现。点击 `A` 后，Notepad 获得一个 `a/A`，前台窗口和焦点不切换到虚拟键盘。

### AC-002 非可编辑元素隐藏

给定键盘正在跟踪输入框，当焦点切换到可获得焦点的按钮或菜单时，键盘自动隐藏。

### AC-003 空白点击语义

给定输入框仍持有键盘焦点，当用户点击不会改变焦点的窗口空白区域时，键盘保持显示；该行为与本规格一致，不判定为缺陷。

### AC-004 手动抑制

给定键盘正在显示，用户点击关闭按钮后键盘保持隐藏；重复焦点通知不得立即重新打开。切换到另一个可编辑输入框后允许再次自动显示。

### AC-005 浏览器输入框

聚焦 Chrome/Edge 的 input 和 textarea 时键盘显示，点击键盘不使浏览器失焦，Unicode 文本和 Backspace 可正常执行。

### AC-006 只读正文不误弹

聚焦或浏览支持 TextPattern 的只读网页正文、文档或控件时，系统不得仅因 TextPattern 存在而显示键盘。

### AC-007 VS Code 定位

聚焦 VS Code 编辑器时，系统能识别有效编辑状态；若能获得 caret 矩形，应按 caret 定位，否则按编辑区或显示器安全位置降级，键盘必须保持完全可见。

### AC-008 多显示器与 DPI

目标输入框位于任一显示器、包括负坐标和混合 DPI 显示器时，键盘出现在同一显示器工作区内；跨屏后尺寸和位置正确更新。

### AC-009 目标切换防误输入

从目标输入框切换到另一应用后立即点击虚拟键盘，旧目标不得收到输入；如果新目标未完成有效识别，该次输入必须取消。

### AC-010 权限边界

普通权限程序面对管理员目标窗口时，不承诺输入成功；失败必须可诊断且不得自动提升权限。

### AC-011 修饰键清理

热键执行、取消、异常和退出后，系统不得遗留由程序造成的 Ctrl、Shift 或 Alt 按下状态。

### AC-012 Unicode

自定义文本包含中日韩字符、重音字符、emoji 和 UTF-16 代理对时，在支持 Unicode 合成输入的普通目标应用中应完整提交，不得被截断为半个代理对。

### AC-013 密码字段

聚焦密码框时仅显示标准安全键；短语键和不安全热键不可用。日志中不存在字段值或输入内容。

### AC-014 配置恢复

配置文件损坏时程序仍能启动，使用默认配置，并保留一份可供排查的损坏文件备份。

### AC-015 单实例与退出

重复启动不会创建第二个监听器；退出后没有遗留托盘图标、UIA 处理器或持续按下的虚拟修饰键。

## 10. MVP 与后续版本边界

### 10.1 MVP 必须交付

- 单实例和托盘控制。
- UIA 焦点检测、可编辑性分类、目标会话和状态机。
- NoActivate WPF 键盘窗口。
- 标准 QWERTY、Unicode 文本、普通键和热键输入。
- caret/控件定位、多显示器和 Per-Monitor DPI。
- JSON 布局、基础设置和可靠配置存储。
- 隐私安全诊断。
- 本文 AC-001 至 AC-015 的验收证据。

### 10.2 后续候选

- 数字键盘、更多语言和布局编辑器。
- 开机启动、主题、动画、贴边和候选词。
- 更完整的触摸手势。
- UIA 工作进程隔离和 provider 硬超时恢复。
- 经安全评审后的 uiAccess 支持。
- 管理员可控的企业策略和部署包。

## 11. 需求变更规则

任何影响以下内容的变更都必须同时更新功能规格、方案设计和开发计划：

- 支持的权限边界或 Windows 版本。
- Editable 判定规则。
- 自动隐藏语义。
- 输入注入方式。
- 配置 schema 或布局 schema。
- MVP 验收场景。

新增需求必须分配唯一 ID，并在方案设计和 TODO 追踪矩阵中建立对应关系。
