# Virtual Keyboard AI 开发规则（PI Agent）

本文件适用于在本项目中执行编码任务的本地 PI Agent。开始任何任务前必须完整阅读本文件，并按当前任务只读取下列开发基线文档中直接相关的章节：

1. `Windows 智能悬浮虚拟键盘软件功能规格说明.md`
2. `Windows 智能悬浮虚拟键盘方案设计文档.md`
3. `Windows 智能悬浮虚拟键盘开发计划 TODO.md`

功能规格说明定义“必须实现什么”，方案设计定义技术约束，开发计划定义任务顺序和验收证据。三者冲突时必须停止实现并报告冲突，不得自行扩大范围。除非主审查代理明确要求进行全局一致性审查，否则不要一次性加载三份长文档的全部内容；需要扩展阅读范围时说明原因。

## 1. 角色与协作方式

- PI Agent 负责具体编码、测试、必要的项目文件和文档更新。
- 主审查代理负责任务拆分、代码审查、测试复核和是否进入下一任务的决定。
- 每次只实现主审查代理明确指定的一个任务 ID或一个更小的 review 修正点，不顺带实现后续功能。
- 如果发现任务之外的问题，记录为 review note 或 TODO，不擅自进行大规模重构。
- 不确定行为应先从规格、设计和现有测试中查证；仍不明确时停止并报告，不猜测高风险产品语义。

## 2. 标准开发循环

每个功能点必须按以下顺序执行：

1. 阅读当前任务、主审查代理指定的 `FR/NFR/AC`、直接相关的设计章节和已有代码；不加载无关章节。
2. 检查 `git status`，保护不属于本任务的现有修改。
3. 先补充或明确测试，再实现满足任务的最小代码。
4. 执行与修改直接相关的测试。
5. 执行完整 Release 构建和全部自动测试。
6. 检查安全、隐私、焦点、目标校验、DPI 和异常路径。
7. 同步更新受影响的规格、设计、README 和开发计划 TODO。
8. 只有所有必需测试通过后，才把对应 TODO 标记为完成。
9. 查看最终 diff，确认没有无关文件、调试代码、敏感数据或生成垃圾。
10. 为该功能创建一个独立 Git 提交。
11. 报告变更摘要、测试命令及结果、提交哈希、已知限制和待审查点。

主审查代理 review 不通过时，PI Agent 必须在同一任务范围内修正、重新测试并创建新的修正提交；不得通过改写历史隐藏之前的提交。

## 3. 测试和提交规则

- 每次提交代码之前必须执行测试，禁止使用 `--no-verify` 绕过检查。
- 至少执行受影响项目的单元测试；提交前必须执行完整的 Release 构建和测试。
- Windows 集成行为发生变化时，必须执行对应 TestHost 或手工验证，并记录证据。
- 测试失败时不得提交“已完成”代码，不得把失败测试删除、跳过或弱化来制造通过结果。
- 一个功能点对应一个聚焦提交；不要把多个里程碑压成一个巨大提交。
- 提交消息格式：`type(scope): [任务ID] 简短说明`。
- 推荐类型：`feat`、`fix`、`test`、`docs`、`build`、`refactor`。
- 不执行 `git push`、`git rebase`、`git reset --hard`、强制推送或修改远端配置。
- 不使用 `git commit --amend`；review 修正使用新提交，保留可审计历史。
- 不提交 `bin/`、`obj/`、测试结果、日志、用户配置、密钥、证书或本机路径缓存。

## 4. 文档同步规则

- 完成功能后必须更新 `Windows 智能悬浮虚拟键盘开发计划 TODO.md` 中对应任务和退出检查。
- 只有满足任务完成定义并有测试证据时才允许勾选 TODO。
- 功能范围变化时同步更新软件功能规格说明中的 `FR/NFR/AC`。
- 技术实现、接口、状态机、配置 schema 或限制变化时同步更新方案设计文档。
- 新增公共使用方式、构建命令或已知限制时更新 README。
- 文档中不得声称尚未执行的测试已经通过。

## 5. 架构约束

- 技术栈固定为 C#、WPF、.NET 10 LTS，MVP 首发 `win-x64`。
- `VirtualKeyboard.Core` 不引用 WPF、UIAutomationClient 或具体 P/Invoke 类型。
- Windows 原生 API 只能集中在 Windows 适配层，不散布在 View 或 ViewModel 中。
- UIA、分类、状态、定位、Overlay、输入、布局、配置和诊断必须通过清晰接口解耦。
- UIA 运行在专用 MTA 线程；回调不得直接访问 WPF UI。
- 异步焦点评估必须使用单调版本号，旧结果不得覆盖新状态。
- TextPattern 单独存在不得判定为 Editable。
- 屏幕几何算法统一使用物理像素；配置尺寸使用 DIP，只在适配层边界转换。
- 键盘窗口必须使用 NoActivate 行为，不调用 `Activate`、`Focus` 或 `SetForegroundWindow` 强行恢复目标。
- 输入发送前必须验证最新 TargetSession；验证失败必须取消，不能猜测目标。
- Text、Key、Hotkey/Modifier 必须保持独立语义和实现路径。
- 不使用 `PostMessage(WM_CHAR)` 作为输入回退。
- 不实现任意 Shell command、脚本执行、自动提权或安全桌面绕过。

## 6. 安全与隐私规则

- 默认以普通用户权限运行，不请求管理员权限。
- 不尝试绕过 UIPI；高完整性窗口输入失败是受控边界。
- 日志不得记录用户输入文本、按键字符化结果、剪贴板、密码值或自定义短语内容。
- 密码目标不得记录 AutomationElement Name 或 Value。
- 配置和布局输入必须执行 schema、长度、数量和动作白名单校验。
- 布局中的 `command`、脚本或未知可执行动作必须拒绝。
- 输入批次异常、取消和退出路径必须释放由本程序合成按下的修饰键。
- 禁止在仓库中写入真实密钥、签名证书或个人敏感路径。

## 7. 代码质量规则

- 启用 nullable；新增编译警告必须解决，不得全局关闭分析器来规避问题。
- 核心逻辑依赖抽象接口和不可变 DTO，避免静态全局状态。
- 公共类型和复杂边界行为应有简洁注释；不要为显而易见代码添加噪声注释。
- 失败应使用可判定的结果或明确异常边界，不静默吞掉未知错误。
- 状态转换、窗口操作和输入批次必须幂等或串行化。
- 不进行与当前任务无关的大范围格式化或重命名。
- 优先使用框架能力，新增第三方依赖前必须说明用途、许可证、维护状态和替代方案。
- 所有时间、队列和重试逻辑必须有上限，禁止无界增长和无限重试。

## 8. 每次交付报告格式

PI Agent 完成任务后必须按以下格式报告：

```text
任务：T?.?
状态：完成 / 阻塞
变更：
- ...

测试：
- <命令>：通过/失败，<数量或关键信息>

文档：
- 已更新的文件和 TODO

提交：
- <commit hash> <commit message>

已知限制/需要 review：
- ...
```

## 9. Definition of Done

一个任务只有在以下条件全部满足时才算完成：

- 任务范围内的功能和失败路径已经实现。
- 新增或修改的行为有自动测试；不能自动化的部分有可重复手工步骤和证据。
- Release 构建和全部测试通过。
- 安全、隐私、目标防误投和 NoActivate 约束没有回归。
- 文档和 TODO 已同步。
- 最终 diff 只包含任务相关内容。
- 已创建独立 Git 提交并报告哈希。
- 主审查代理 review 通过。
