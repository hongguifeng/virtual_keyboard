# Changelog

## 1.0.6 — 2026-09-09

- REL-029：同一输入框暂时识别失败后最多追加 4 次重试；统一采集和评估的 UIA/原生回退，保留目标身份与可编辑性校验。
- 分类日志增加具体失败阶段、原因、耗时、焦点版本、重试次数和回退标志；新增重试安排、耗尽和恢复事件，不记录输入内容。
- Release 构建零警告；Core 236、Windows 224、Integration 56（516）通过，TestHost 自检退出码 0，发布校验通过。
- 修复候选版本；跨 VS Code 与其他软件的用户复测、主审查仍待完成。

## 1.0.5 — 2026-09-07

### 修复

- 应用刚启动（键盘从未弹出、主窗口从未 `Show`）时从托盘右键菜单“打开设置”失败（1.0.4 用户反馈）：WPF 禁止把从未显示的窗口设为 Owner（`InvalidOperationException`），原实现无条件 `Owner = this`，导致必须等键盘弹出过一次后托盘设置入口才可用。修复：`OpenSettingsWindow` 仅在主窗口已显示（`IsLoaded`）时设置 Owner，否则设置窗口作为独立顶层模态窗口打开，后续行为不变。新增 2 个集成回归测试（未显示主窗口：不抛异常、Owner 为 null；已显示主窗口：Owner 保持），并已验证新测试对旧实现失败（`InvalidOperationException`）。
- 完整 Release 门禁：Core 236、Windows 218、Integration 48（共 502）全部通过，构建 0 warning / 0 error。

### 发布状态

- 与 1.0.4 相同的限制：制品未签名，仅限内部测试。

## 1.0.4 — 2026-09-06

### 已实现

- 新增“开机启动”设置（FR-APP-004，T6.7）：登录 Windows 时自动启动。启用写入当前用户 `HKCU\...\Run` 值 `VirtualKeyboard`（带引号的当前可执行文件路径，无需管理员权限），禁用删除；首次启动默认关闭。
- 注册表是自启状态的事实来源，`config.json` 的 `autoStart`（旧版文件缺失时默认 false，不升 schema）仅为镜像；应用启动时自动将注册表真实状态同步到配置（兼容任务管理器等其他位置修改）。
- 设置窗口新增“开机启动”复选框：保存后应用，写入/复核失败时回滚镜像、取消勾选并显示警告，窗口保持打开由用户决定重试或取消。
- 诊断新增 `AutoStartSync` 数字事件；自启路径不记录注册表/进程路径（NFR-PRI-001）。
- 测试：Core +2（往返/旧文件默认）、Windows +6（AutoStartManager 假接缝单测）、Integration +6（设置与主窗口集成），完整 Release 门禁 Core 236 / Windows 218 / Integration 46（共 500）全部通过。

### 发布状态

- 与 1.0.3 相同的限制：制品未签名，仅限内部测试；真实注册表自启行为（登录后自动拉起、与任务管理器联动）仍需 M7/手工验证。

## 1.0.3 — 2026-07-25

### 修复

- 设置窗口“自定义按键”组所在的行由等权 `*` 改为 `Auto`（表单区全部行不再使用 `*`）：等权 `*` 行在窗口实际高度下把自定义按键组压缩到约 74 DIP（内容约 340 DIP），内容溢出并覆盖下方“拖动结束后保留位置”设置项（1.0.2 用户反馈的严重重叠）。
- 集成测试强化：自定义按键组压缩检测 + 24 个命名控件两两无交叠断言，并验证对 1.0.2 布局会失败。

## 1.0.2 — 2026-09-06

### 修复

- 设置窗口简体中文下“录制按键”按钮被两行提示文字覆盖(REL-026):窗口高度改为随内容自适应(`SizeToContent=Height`),自定义键列表超出 210 DIP 时内部滚动;新增集成测试防止按钮/提示文字重叠回归。

### 发布状态

- 与 1.0.0 相同的限制:制品未签名,仅限内部测试。

## 1.0.1 — 2026-09-06

### 已实现

- 新增应用图标 `Assets/app.ico`(多尺寸 PNG 压缩,匹配现代主题:深色圆角底、蓝色状态点、键帽与空格键)。
- 图标接入三处:exe 文件图标、WPF 主窗口/设置窗口图标、系统托盘(通知区)NotifyIcon。
- 托盘图标从嵌入资源加载,资源缺失时回退系统默认图标。

### 发布状态

- 与 1.0.0 相同的限制:制品未签名,仅限内部测试。

## 1.0.0-internal — 2026-09-06

### 已实现

- .NET 10 / WPF `win-x64` 工程、统一 Release 构建和三组自动测试。
- NoActivate Overlay、UIA/Win32 目标模型、三态分类、物理像素定位算法和 DPI 消息处理。
- 有界输入队列、目标复核、Unicode/Key/Hotkey 独立发送、UIPI 诊断和修饰键安全闩锁。
- 版本化 JSON 布局、内置 QWERTY、状态键、密码策略和触摸事件路径。
- 配置仓库、设置窗口、托盘菜单、每用户会话单实例和有序退出。
- 框架依赖便携 ZIP、SHA-256 和发布安全静态校验。

### 发布状态

- M7 按项目决策跳过，其任务均未完成。
- 15 个 AC 中 0 个具有完整实机通过证据；当前候选版被发布评审拒绝。
- 自动焦点观察/分类/定位尚未接入 App 宿主，磁盘诊断尚未接入。
- 缺少 Windows 10、跨应用、多屏/混合 DPI、管理员、稳定性、性能和实体触摸证据。
- 制品未签名，且当前环境无法完成有效恶意软件扫描；仅限内部测试。

完整阻断列表见 `docs/release/known-issues-1.0.0.md`。

### 发布评审后修正

- `REL-001`：接通 UIA 焦点观察、模式证据分类、版本化目标会话、DPI/工作区定位、自动显示/隐藏和手动抑制；状态改为 Verification，仍需真人兼容矩阵。
- `REL-006`：接通 20 MiB 有界 JSONL 文件诊断和运行时详细开关，补充只导出日志文件的隐私支持步骤；仍需 M7 独立隐私审计。
- `REL-019`：普通键改为在有效点击完成后以同一个扫描码 `SendInput` 批次提交 Down/Up，避免一个按键生命周期跨越中文 IME 候选窗状态变化；等待真人鼠标复验。
- `REL-020`：自动定位为 IME 候选窗预留 96 DIP 的垂直安全区，避免微信输入法候选窗覆盖键盘上排并抢走点击；不读取候选内容，手动位置保持原语义。
- `REL-021`：Backspace 支持有上限的渐进加速长按删除；短按仍为一次，取消或松开立即停止，慢目标不会产生积压删除。
