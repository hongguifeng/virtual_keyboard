<UserControl x:Class="VirtualKeyboard.App.FocusDiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Focusable="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="UIA 判定诊断" FontWeight="SemiBold" FontSize="16" />
        <TextBlock x:Name="ClassificationText" Grid.Row="1" Margin="0,8,0,0" Text="尚无焦点判定" />
        <TextBlock x:Name="IdentityText" Grid.Row="2" Margin="0,4,0,0" Text="版本 - · PID - · HWND -" />
        <TextBlock x:Name="MetadataText" Grid.Row="3" Margin="0,4,0,0" Text="控件 Unknown · 降级 False" />
        <TextBlock Grid.Row="4" Margin="0,10,0,0" Foreground="#666666"
                   Text="诊断仅包含枚举、数字和布尔元数据，不包含控件名称、Value 或输入文本。" />
    </Grid>
</UserControl>
